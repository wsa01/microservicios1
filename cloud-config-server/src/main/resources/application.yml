
server:
  port: 8081
 
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/wsa01/microservicios1.git
          search-paths:
          - config-data
          username: wsa01
          password: Billy199459_GH
  security:
    user:
      name: root
      password: secret